import React from 'react';
import ReactDOM from 'react-dom';

import ArticlesList from './ArticlesList';
// JSON.parse(document.getElementById('ar').getAttribute('data-json'))
ReactDOM.render(<ArticlesList />, document.getElementById('articlesList'))   